/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package br.com.tabela.codigoEspecifico;

import java.math.BigInteger;
import java.util.Scanner;

/**
 *
 * @author Ricardo Borba de Oliveira
 */
/*public class numPositivos {
    //public static boolean divisivel(BigInteger numero, String divisor) {
    //  return numero.mod(new BigInteger(divisor)).equals(BigInteger.ZERO);
           /*public void insertString(int offset, String str, AttributeSet attr)
        throws BadLocationException{
            if(str == length == 1){
                str = str.replace("[^-0-9]",""); //primeiro caractere deve ser hífen ou números
            }else if(str.length == 2){
                str = str.replace("[^0-9]",""); //segundo caractere só pode ser número
            }else if(str.length >= 3){
                str = str.replace("[^0-9]",""); //adicionado para caso o usuário pressione duas teclas ao mesmo tempo e uma delas não seja número
            }
            return str;
        }

*/
